@layout('layout.app')

@section('content')

<div class=" w-3/4 mx-auto flex justify-center items-baseline mb-8">
    <span class="text-xl text-gray-700 font-semibold ml-5">Your Feed</span>
    <form action="refresh" method="GET" class="ml-auto mr-5">
        <button type="submit" class="text-blue-500">Refresh</button>
    </form>
</div>

<div class="w-3/4 mx-auto">
    @if(articles.length)
        @each(article in articles)
            <a href={{ article.article_url }} target="_blank" class="p-10 bg-gray-200 hover:bg-gray-300 rounded-lg mb-5 block">
                @if(article.image_url)
                    <img src={{ article.image_url }}>
                @endif
                <div>
                    <h2 class="mb-5 -mt-5 text-right">{{ article.feed.title }}</h2>
                    <h3 class="font-bold text-xl mb-3">{{ article.title }}</h3>
                    {{ article.description }}
                </div>
            </a>
        @endeach  
    @else  
        <h3 class="text-center">No feeds added yet! Click <a href="/feeds/create" class="font-semibold text-blue-400">here</a> to add a feed!</h3>
    @endif
</div>

@endsection